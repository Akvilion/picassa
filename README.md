# Pica3

Демонстрація прозорого вікна з ефектом матового скла (glassmorphism) на базі Wails v2.

## Опис

Цей проект демонструє, як створити десктопний застосунок з прозорим вікном та ефектом розмитого скла, через яке видно обої робочого столу. Використовується фреймворк Wails для створення нативних застосунків з веб-технологіями (HTML/CSS).

## Особливості

- ✨ Повністю прозоре вікно
- 🎨 Ефект матового скла (glassmorphism)
- 💻 Кросплатформність (Windows, macOS)
- 🪟 Нативна інтеграція з ОС
- ⚡ Легкий та швидкий

## Вимоги

- Go 1.18 або новіше
- Wails CLI v2
- Windows 10/11 або macOS

## Встановлення Wails

```bash
go install github.com/wailsapp/wails/v2/cmd/wails@latest
```

## Запуск у режимі розробки

### Використовуючи Makefile (рекомендовано)

```bash
make dev
```

### Або напряму через Wails

```bash
wails dev
```

Застосунок запуститься з гарячим перезавантаженням (hot reload).

## Збірка production-версії

### Використовуючи Makefile

```bash
make build
```

### Або напряму через Wails

```bash
wails build
```

Готовий виконуваний файл буде у папці `build/bin/`.

## Корисні команди Makefile

```bash
make help          # Показати всі доступні команди
make dev           # Запустити у режимі розробки
make build         # Зібрати production версію
make build-debug   # Зібрати debug версію
make clean         # Очистити build директорію
make test          # Запустити тести
make fmt           # Форматувати код
make lint          # Перевірити код лінтером
make check         # Запустити всі перевірки
```

## Налаштування прозорості

### Go (main.go)

```go
BackgroundColour: &options.RGBA{R: 0, G: 0, B: 0, A: 0}, // A: 0 = повністю прозорий

Windows: &windows.Options{
    WebviewIsTransparent: true,
    WindowIsTranslucent:  true,
}
```

### CSS (style.css)

```css
html, body {
    background: transparent !important;
}

.glass-card {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(20px) saturate(180%);
    border-radius: 20px;
}
```

## Параметри налаштування

### Прозорість фону картки

У файлі `frontend/dist/style.css` змініть значення `rgba()`:

```css
background: rgba(255, 255, 255, 0.15);
/* 0.15 = 15% непрозорості
   0.5 = 50% непрозорості
   1.0 = 100% непрозорості (непрозорий) */
```

### Сила розмиття

```css
backdrop-filter: blur(20px);
/* 10px = легке розмиття
   20px = середнє розмиття
   40px = сильне розмиття */
```

### BackdropType для Windows

У `main.go` можна використовувати різні ефекти:

```go
// Закоментовано для повної прозорості
// BackdropType: windows.Acrylic,  // Ефект Acrylic
// BackdropType: windows.Mica,     // Ефект Mica (Windows 11)
// BackdropType: windows.Tabbed,   // Ефект Tabbed (Windows 11)
```

**Примітка:** `BackdropType` може блокувати повну прозорість. Для максимальної прозорості залиште його закоментованим.

## Усунення проблем

### Вікно не прозоре

1. Перевірте налаштування Windows:
   - Налаштування → Персоналізація → Кольори
   - Увімкніть "Ефекти прозорості"

2. Перезапустіть застосунок повністю (закрийте та запустіть знову)

3. Спробуйте зібрати production-версію:
   ```bash
   wails build
   ./build/bin/pica3.exe
   ```

### Ефект розмиття не працює

- На Windows 10 потрібна версія 1809 або новіша
- На Windows 11 ефект працює краще
- Переконайтеся, що у браузері підтримується `backdrop-filter`

### Вікно показує білий фон

Переконайтеся, що у `style.css` є:

```css
html, body {
    background: transparent !important;
}
```

## Структура проекту

```
pica3/
├── main.go                    # Точка входу в застосунок
├── internal/                  # Внутрішня логіка (недоступна ззовні)
│   └── app/
│       └── app.go             # Основна логіка застосунку
├── pkg/                       # Публічні пакети (можуть використовуватись іншими проектами)
│   └── config/
│       └── window.go          # Конфігурація вікна та налаштування
├── frontend/                  # Фронтенд застосунку
│   ├── dist/
│   │   ├── index.html         # HTML розмітка
│   │   └── style.css          # CSS стилі з ефектом скла
│   └── wailsjs/               # Згенеровані Wails JS біндінги
├── build/                     # Збудовані виконувані файли (ігнорується git)
├── go.mod                     # Go модулі
├── wails.json                 # Конфігурація Wails
├── .gitignore                 # Git ignore правила
└── README.md                  # Цей файл
```

### Опис директорій

- **internal/app/** - Внутрішня бізнес-логіка застосунку. Код тут недоступний для імпорту з інших проектів.
- **pkg/config/** - Публічні пакети з конфігурацією. Можуть використовуватись іншими частинами проекту або зовнішніми проектами.
- **frontend/** - Веб-інтерфейс застосунку (HTML, CSS, JavaScript).
- **build/** - Директорія для зібраних бінарних файлів (створюється автоматично при збірці).

### Архітектура коду

Проект організований за принципами чистої архітектури Go:

1. **main.go** - мінімальна точка входу, яка:
   - Ініціалізує застосунок
   - Завантажує конфігурацію
   - Запускає Wails runtime

2. **pkg/** - публічні пакети:
   - Можуть імпортуватись іншими проектами
   - Містять утиліти та конфігурацію
   - Без зовнішніх залежностей від internal/

3. **internal/** - приватна логіка:
   - Недоступна для імпорту ззовні
   - Містить бізнес-логіку
   - Методи, які можна викликати з фронтенду

## Технології

- [Wails v2](https://wails.io/) - Фреймворк для створення десктопних застосунків
- [Go](https://golang.org/) - Бекенд
- HTML/CSS - Фронтенд

## Ліцензія

MIT

## Автор

Створено з використанням Wails v2

## Корисні посилання

- [Документація Wails](https://wails.io/docs/introduction)
- [Wails на GitHub](https://github.com/wailsapp/wails)
- [Приклади Wails](https://wails.io/docs/examples/showcase)
