# Швидкий старт Pica3

## Передумови

Переконайтеся, що у вас встановлено:
- Go 1.18+ ([завантажити](https://golang.org/dl/))
- Node.js (опціонально, для frontend розробки)
- Wails CLI v2

## Крок 1: Встановлення Wails CLI

```bash
go install github.com/wailsapp/wails/v2/cmd/wails@latest
```

Перевірте встановлення:
```bash
wails version
```

## Крок 2: Встановлення залежностей

```bash
# У кореневій директорії проекту
cd pica3
go mod download
```

Або через Makefile:
```bash
make install-deps
```

## Крок 3: Запуск у режимі розробки

```bash
make dev
```

Або:
```bash
wails dev
```

Застосунок відкриється у вікні з прозорим фоном!

## Крок 4: Збірка production версії

```bash
make build
```

Виконуваний файл буде у `build/bin/pica3.exe` (Windows) або `build/bin/pica3` (macOS/Linux).

## Перше знайомство з кодом

### Де що знаходиться?

1. **Точка входу**: [main.go](main.go)
   - Ініціалізація застосунку
   - Завантаження конфігурації

2. **Бізнес-логіка**: [internal/app/app.go](internal/app/app.go)
   - Методи застосунку
   - Життєвий цикл

3. **Конфігурація**: [pkg/config/window.go](pkg/config/window.go)
   - Налаштування вікна
   - Параметри прозорості

4. **UI**: [frontend/dist/](frontend/dist/)
   - HTML розмітка
   - CSS з ефектом скла

### Як додати свій метод?

1. Відкрийте `internal/app/app.go`
2. Додайте метод до структури `App`:

```go
// Приклад: метод для отримання поточного часу
func (a *App) GetCurrentTime() string {
    return time.Now().Format("15:04:05")
}
```

3. Метод автоматично стане доступним на фронтенді!

### Як змінити прозорість?

Відкрийте `frontend/dist/style.css` та змініть:

```css
.glass-card {
    background: rgba(255, 255, 255, 0.15); /* 0.15 = 15% непрозорості */
    backdrop-filter: blur(20px); /* сила розмиття */
}
```

## Типові проблеми

### Вікно не прозоре?

1. Переконайтеся, що у Windows увімкнено ефекти прозорості:
   - Налаштування → Персоналізація → Кольори → Ефекти прозорості

2. Перезапустіть застосунок повністю

3. Спробуйте production збірку:
   ```bash
   make build
   ./build/bin/pica3.exe
   ```

### Помилка компіляції?

```bash
# Очистіть та перевстановіть залежності
go clean -cache
go mod tidy
go mod download
```

### Wails не знайдено?

Переконайтеся, що `$GOPATH/bin` (або `%GOPATH%\bin` на Windows) у змінній PATH:

**Windows:**
```powershell
$env:PATH += ";$env:GOPATH\bin"
```

**Linux/macOS:**
```bash
export PATH=$PATH:$(go env GOPATH)/bin
```

## Наступні кроки

1. Прочитайте [README.md](README.md) для детальної документації
2. Вивчіть [ARCHITECTURE.md](ARCHITECTURE.md) для розуміння структури проекту
3. Почніть додавати свій функціонал!

## Корисні посилання

- [Документація Wails](https://wails.io/docs/introduction)
- [Приклади Wails](https://wails.io/docs/examples/showcase)
- [Go документація](https://golang.org/doc/)

## Команди Makefile

```bash
make help          # Показати всі команди
make dev           # Режим розробки
make build         # Production збірка
make test          # Запустити тести
make clean         # Очистити build
make fmt           # Форматувати код
make lint          # Лінтинг
make check         # Всі перевірки
```

Успішної розробки! 🚀
